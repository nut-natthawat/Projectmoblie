import SwiftUI

struct AppColors {
    
    // ----------------------------------------
    // โทนสี High-Vis Energy (สำหรับ Dark Mode)
    // ----------------------------------------
    
    /// พื้นหลังหลัก / Text (สำหรับ Dark Mode)
    static let dark = Color(hex: "0b0d17")
    
    /// สีเน้นสว่าง (Accent Color) เช่น ปุ่ม Start Run / เส้น GPS
    static let hotPink = Color(hex: "ff0066")
    static let babyneonGreen = Color(hex: "CEFF78")
    
    // ----------------------------------------
    // โทนสี Dynamic & Composed (สำหรับ Light Mode)
    // ----------------------------------------

    /// พื้นหลังหลัก / Background (สำหรับ Light Mode)
    static let light = Color(hex: "f5f7fa")
    static let white = Color(hex: "ffffff")
    /// สีเน้นทางเลือก (Accent Color) เช่น เส้น GPS / กราฟ
    static let vibrantTeal = Color(hex: "70BBA0")
    
    // ----------------------------------------
    // ตัวอย่างสีอื่นๆ ที่เราเคยคุยกัน
    // ----------------------------------------

}
